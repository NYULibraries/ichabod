<!--partial for _document and _show_default views, which adds 'Edit' and 'Destroy' buttons for authorized users
added through config.index.partials and config.show.partials in catalog_controller-->
<%= render 'buttons', item: Nyucore.find(document.id), search_params: request.params %>
